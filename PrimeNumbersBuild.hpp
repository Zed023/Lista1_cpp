#pragma once
#include <string>
#include <vector>
#include <math.h>
using namespace std;

class PrimeNumbers
{
    
    public:
    vector<bool> primes;
    int n;
    int m;

    PrimeNumbers(int n1);
    unsigned int liczba(int m1);
};
